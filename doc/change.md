请根据以下要求，重构我项目中的“系统设置 / AI 助教配置页面”的排版与样式。**背景颜色必须保留我项目原有的背景色（不改为浅色）**，所有卡片、分割线、行高、圆角、开关、右箭头等元素需采用下方描述的通用设置风格。**严禁改变任何按钮、开关、上传组件、下拉选择等元素对应的后端接口路径、请求参数、业务逻辑或原有功能，只允许改变视觉排版和样式。** 原有的事件绑定、数据提交、API 调用等必须保持完全不变。

### 一、整体布局与容器

- 页面标题保持现有“AI助教”或“系统设置”，位于顶部左对齐（可保留原有品牌区域）。
- 使用**单列滚动布局**，内容区最大宽度 **900px**（因内容较多），居中显示。
- 背景色：**完全保留当前项目的背景色**（不强制改为浅色背景，请读取现有 `body` 或容器背景并保持不变）。
- 所有设置项分组为**圆角卡片**（如果原背景为深色模式，卡片使用 `#1C1C1E` 或半透深色卡片；如果原背景为浅色，卡片使用白色 `#FFFFFF`）。卡片圆角 **16px**，内边距 0，轻微阴影。
- 分组之间间距 **24px**。
- 每组有分组标题，字号 **16px**，字重 **500**，颜色与当前主题文字颜色一致，位于卡片内部顶部（留白 16px 并显示标题）。

### 二、每个设置项的样式（卡片内部）

- 每个设置项为**一行**，左右布局：
  - 左侧：标签文字，字号 **16px**，字重 **450**，颜色跟随主题（深色下 `#F0F0F0`）。
  - 右侧：控件或状态文字 + 可能的右箭头 / 开关 / 按钮。
- 行高 **56px**，左右内边距 **20px**。
- 项之间有 **1px** 分割线（浅色模式 `#E9ECEF`，深色模式 `#2C2C2E`），最后一项无分割线。
- 右侧控件文字字号 **15px**，颜色 `#6C6C70`（深色模式下 `#8E8E93`）。

### 三、各类右侧控件的统一样式（仅改样式，不改功能）

1. **开关（Toggle）**
   - 宽度 **44px**，高度 **24px**，圆角 **12px**，圆形滑块直径 20px。
   - 开启背景 `#007AFF`，关闭背景 `#C4C4C4`（深色模式下关闭背景 `#3A3A3C`）。
   - 用于“启用重排序”“自动导入知识库”“VLM图片描述”等。
   - **必须保留原有开关的绑定状态和 change 事件，只修改视觉样式**。
2. **带右箭头（Chevron）的选择项**
   - 右侧显示当前选择的值，紧跟着右箭头图标（`›` 或 `>`）。
   - 整个项可点击（hover 背景色变化），点击后触发原有的下拉或弹窗逻辑。
   - 用于模型选择、重排序模型选择等。**原有的数据源和选择后的保存逻辑不得改动**。
3. **纯文本状态**
   - 例如“状态：就绪”“文档数：385”“分块数：8295”等统计信息，右侧显示只读数值，无箭头。
   - 仅改变文字颜色和布局，**数据来源和刷新机制保持不变**。
4. **按钮（主要操作 / 次要操作）**
   - “启动AI引擎” / “停止AI引擎” —— 使用主按钮样式（填充 `#007AFF`，白色文字，圆角 8px，内边距 8px 20px）。
   - 停止按钮可使用红色或灰色边框按钮。
   - **按钮的点击事件、后端请求地址、请求参数必须原样保留**。
5. **上传区域**
   - “拖拽文件到此处或点击上传” —— 保留原有上传区域的所有功能（拖拽、点击选择、文件校验、上传接口等），仅调整其视觉为圆角 12px 的虚线边框卡片，背景透明或半透明。
6. **滑块或数值调节**
   - “Top K” 和 “初始检索数量” —— 如果原界面支持直接输入或滑块，必须保留原有交互逻辑；如果原界面无调节方式，仅显示数值，则改为带右箭头的只读项（点击后弹出原有调节界面）。**不能破坏原有数值的读取和存储逻辑**。

### 四、页面内容分组（保持现有功能，只重新排版）

请将当前截图中所有功能元素提取出来，按照下方分组顺序重新排版。**每个设置项的原有的** **`id`、`name`、数据绑定属性、事件监听器必须完整保留，只移动它们在 DOM 中的位置和样式**。

#### 分组 1：AI引擎控制

- **引擎状态**（右侧：显示“系统运行中”或“引擎已启动”，纯文本）
- **当前模型**（右侧：显示当前选中的模型名，带右箭头，点击后触发原有模型切换逻辑）
- **重排序状态**（右侧：显示“已启用”或“未启用”，带右箭头，点击跳转到重排序配置或展开面板）
- **操作**（右侧：放置“启动AI引擎”或“停止AI引擎”按钮，根据状态显示其中一个，按钮的原有 `onclick` 或数据绑定必须保留）

#### 分组 2：多模态配置

- **VLM图片描述**（右侧：开关。开关原有状态和事件完整保留。可显示小字描述：“启用后将使用视觉语言模型对图片生成详细描述”，该描述作为辅助文字放在标签下方）
- **图片描述模型**（右侧：当前选中的模型名 + 右箭头，点击后触发原有模型选择逻辑）
- **知识库统计**（右侧：显示“文档数: XXX | 分块数: XXX | 图片块数: X”，纯文本，数据动态刷新方式不变）

#### 分组 3：多模态知识库管理

- **上传文件**：左侧标签，右侧为原有上传组件区域。必须保持原有的拖拽上传、点击上传、文件格式限制、大小限制、上传接口 URL 等全部不变。
- **知识库映射说明**：右侧显示“文档 → 主知识库 | 图片 → 多模态知识库（OCR）”，只读文本。

#### 分组 4：重排序配置

- **启用重排序**（右侧：开关，保留原有开关状态和事件）
- **重排序模型**（右侧：当前选中的模型名 + 右箭头，保留原有选择逻辑）
- **Top K**（右侧：当前数值 + 右箭头，点击后使用原有的数值调节方式）
- **初始检索数量**（右侧：当前数值 + 右箭头，保留原有调节逻辑）

#### 分组 5：其他设置（若有）

- **自动导入知识库**（右侧：开关，保留原有开关行为）
- **系统运行状态指示灯**（可整合到 AI引擎控制分组或单独作为状态行）

### 五、重要约束（功能保护）

- **严禁修改任何后端接口路径、请求方法、参数名称、鉴权方式**。
- **严禁修改任何现有 JavaScript 函数、Vue/React 组件的方法、数据模型绑定**。
- **严禁修改任何表单提交的 URL 或数据格式**。
- **严禁修改上传组件的** **`action`** **或** **`url`** **属性**。
- **严禁修改开关、按钮、下拉菜单等元素的** **`name`、`id`、自定义属性（如** **`data-*`）**，因为这些可能用于前端逻辑或后端数据绑定。
- 如果原页面使用了框架（如 Vue、React），重构时必须保持相同的组件结构和生命周期钩子，只更改模板中的布局和 CSS 类名。
- 在重构过程中，如果无法确认某个元素是否影响功能，**保持其原始 HTML 结构和属性不变**，仅在外层包裹样式容器或调整 CSS 布局。

### 六、交互与样式要求

- 所有带右箭头的项，鼠标移入背景色变化（`#F2F2F5` 或深色下 `#2C2C2E`），光标为 pointer。
- 开关可交互（保持原有切换功能）。
- 按钮 hover 效果。
- 上传区域保持原有功能。
- 页面需支持垂直滚动，响应式（移动端内边距缩小为 12px）。

### 七、技术实现

- 请使用 **HTML + CSS + 原生 JavaScript**（若项目为 React/Vue，请根据当前技术栈输出对应组件代码，但必须保留原有数据绑定和事件处理）。
- **不引入外部图标库**，右箭头使用 Unicode `›` 或 SVG，开关纯 CSS。
- **保留当前项目的背景色变量**（从现有代码中继承，不覆盖为浅色）。

### 八、输出要求

请直接输出完整的设置页面代码（单个文件），覆盖现有的“系统设置”视图。代码中**所有原有功能点必须 100% 可用**，仅视觉排版和样式按照上述卡片、分组、行布局改变。
